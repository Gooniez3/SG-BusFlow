# SG BusFlow web

Next.js client for the FastAPI API. Setup, environment variables, and screenshots are in the [root README](../../README.md).

```powershell
npm install
npm run dev
```

http://127.0.0.1:3000 talks to `NEXT_PUBLIC_API_URL` (default `http://127.0.0.1:8000`).
