# Copy to .env in this folder. Never commit .env.
# Docker Compose injects this file into the API and worker.
# Inside those containers, DATABASE_URL and REDIS_URL are overridden
# to postgres:5432 and redis:6379. The localhost URLs below are for
# running FastAPI or the worker on the host against Compose.

DATABASE_URL=postgresql+psycopg://busflow:busflow@localhost:5433/busflow
REDIS_URL=redis://localhost:6379/0
LTA_ACCOUNT_KEY=
LTA_BASE_URL=https://datamall2.mytransport.sg/ltaodataservice
LTA_TIMEOUT_SECONDS=10
ARRIVAL_CACHE_TTL_SECONDS=30
STATIC_CACHE_TTL_SECONDS=86400
LTA_WATCH_STOPS=
ARRIVAL_POLL_INTERVAL_SECONDS=20
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001,http://localhost:8081,http://127.0.0.1:8081
GROQ_API_KEY=
GEMINI_API_KEY=
OPENAI_API_KEY=
OPENAI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai
OPENAI_MODEL=gemini-3.6-flash
RATE_LIMIT_PER_MINUTE=120
RATE_LIMIT_ASSISTANT_PER_MINUTE=20
RATE_LIMIT_JOURNEY_PER_MINUTE=40
WS_MAX_CONNECTIONS=400
WS_MAX_CONNECTIONS_PER_IP=32
WS_IDLE_SECONDS=90
LTA_RETRY_ATTEMPTS=2
STALE_AFTER_SECONDS=90
LOG_JSON=false
EXPOSE_DOCS=true
